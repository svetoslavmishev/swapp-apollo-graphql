import React from 'react';
import { Header } from '../index';

const Protected = () => {
  return (
    <div>
      <Header />
      Hello from Protected route
    </div>
  );
};

export default Protected;
